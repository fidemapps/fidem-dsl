challenge_availability_rules
         ::= 'belongs to smartlist'  '{smartlist code}'  ( ','  '{smartlist code}' )*
           | 'challenge'  '{challenge code}'
           | 'in zone'  '{zone code}' (  ','  '{zone code}' )* (  'for'  'NUMBER'  'minutes, hours, days, weeks, months, years' )?
           | 'level'  '{level code}' '>=, <=, =, >, <'  'NUMBER'
           | 'tag'  '{tag code}' '>=, <=, =, >, <' 'NUMBER'