language: node_js

node_js:
  - 0.10.30

env:
  global:
    - XUNIT_FILE=shippable/testresults/result.xml

before_install:
  - npm install -g grunt-cli
  - npm install -g pegjs

before_script:
  - mkdir -p shippable/testresults

script:
  - grunt pegjs
  - npm test
